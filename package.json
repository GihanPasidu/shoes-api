{
    "name": "json-server",
    "version": "1.0.0",
    "main": "server.js",
    "dependencies": {
      "json-server": "^0.17.0",
      "concurrently": "^8.0.0"
    },
    "scripts": {
      "start": "npx concurrently \"npx json-server --watch db.json --port 5000\" \"npx json-server --watch user.json --port 5001\""
    }
  }
  